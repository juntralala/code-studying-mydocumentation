/** HTTP Header
 * HTTP Header merupakan informasi tambahan yang biasa dikirim di Request atau Response (HTTP Message)
 * HTTP Header biasanya digunakan agar informasi tidak harus dikirim melalui Request Body atau Response Body
 * HTTP Header berisi key : value, dan saat ini sudah banyak sekali standarisasi nama key pada HTTP Header
 * https://en.wikipedia.org/wiki/List_of_HTTP_header_fields
 */

// namun kita tidak wajib hafal semua key http header, karna biasanya sudah ditangani otomatis oleh browser
// kita cuman perlu tau saja