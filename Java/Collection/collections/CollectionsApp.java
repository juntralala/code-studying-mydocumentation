package collections;

/**
 * Collections adalah class di java yang memiliku utility static method untuk membantu kita menggunakan static method
 * di materi materi sebelumnya kita sudah membahas beberapa, seperti membuat imutable collections misalnya
 * namun sebenarnya masih banyak static method yang bisa kita gunakan di class Collections
 */

/** beberapa method Collections class
 * copy(list1, list2) : void
 * frequency(collection, object) : int                      menampilkan berapa banyak element yang sama dengan parameter
 * max(collection) atau max(collection, comparable)         mengambil element tertinggi di list
 * min(collection) atau min(collection, comparable)         mengambil element terendah di list
 * reverse(list) : void
 * shuffle(list) : void                                     mengacak
 * swap(list, from, to) : void
 * DLL
 */

public class CollectionsApp {
    public static void main(String[] args) {

    }
}
