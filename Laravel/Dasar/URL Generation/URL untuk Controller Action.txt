/** URL untuk Controller Action
 * URLGenerator juga bisa digunakan untuk membuat link menuju controller action
 * Laravel secara otomatis akan mencarikan path yg sesuai di route dengan controller action tersebut 
 * Kita bisa menggunakan method action(controllerAction, parameters), atau URL::action(controllerAction, parameters)
   atau url()->action(controllerAction, parameters)
 */