/**
 * Script di package.json terdapat beberapa yang spesial atau khusus
 * Script tersebut tidak perlu dijalankan menggunakan npm run-script namascript, tapi bisa langsung dijalankan
   menggunakan perintah npm namascript
 * Contoh special script yaitu : start, stop, test, restart, uninstall, version, dan lain-lain
 * Selain itu, terdapat script spesial untuk script diatas, kita bisa gunakan gunakan prefix pre sebagai script
   yang akan dijalankan sebelumnya, dan prefix post sebagai script yang dijalankan setelahnya
 * Misal ketika kita gunakan perintah npm start, maka akan menjalankan script prestart, start dan poststart
 */
