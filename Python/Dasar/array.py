from array import array 

# cara membuat array
arr = array("i", [2, 8, 4, 9])
print(arr)

# Array default value
arr = array("i", [0 for i in range(10)])        # huruf i menunjukkan tipe data integer
print(arr)
