<?php

/**
 * Function createMock(class) hanya bisa digunkan untuk class dan interface, maka jika kita mau membuat
   mock object dari data Trait atau Abstract Class, maka kita perlu membuat menggunkan function yang beda
   dari sebelumnya
 * Function getMockForTrait(trait) untuk mock object dari trait
 * FUnction getMockForAbstract(abstractClass) untuk membuat mock object dari abstract class
 */

 // tidak ada contoh 