/**
 * Saat menggunakan @dataProvider, kita harus membuat function yang mengembalikan data
 * Sebenarnya ada cara lebih mudah tanpa harus membuat function terlebih dahulu, yaitu menggunakan
   annotation @testWith
 * Dengan menggunakan @testWith kita bisa mengirim data ke unit test tanpa harus membuat function, kita
   cukup memasukkan data di dalam doc nya saja
*/

// Contoh @testWith ada di MathTest.php  