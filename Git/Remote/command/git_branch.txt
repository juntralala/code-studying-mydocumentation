git branch -r                                                        <- melihat semua Remote Branch        <- -r = remote
git branch -a                                                        <- melihat semua branch dari local dan remote  <- -a = all